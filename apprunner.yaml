version: 1.0
runtime: nodejs12
build:
  commands:
    build:
      - npm install
      - npm run build
      - cd Server
      - npm install
run:
  command: node Server/index.js
  network:
    port: 80
