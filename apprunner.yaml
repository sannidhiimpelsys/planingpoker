version: 1.0
runtime: nodejs12
build:
  commands:
    build:
      - npm install
      - npm run build
      - cd ./server
      - npm install
      - cd ~
run:
  command: node server/index.js
  network:
    port: 80
